<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title ? title + " · Admin" : "Painel Admin" %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root{--bg:#0f1320;--panel:#141a2a;--muted:#8e9aac;--txt:#e6e9ef;--brand:#7c5cff;--ok:#20c997;--warn:#ffc107;--bad:#ff6b6b;--line:#232a3d}
    *{box-sizing:border-box;font-family:Poppins,system-ui,Segoe UI,Roboto,Helvetica,Arial}
    html,body{margin:0;height:100%}
    body{display:flex;min-height:100vh;background:linear-gradient(180deg,#0f1320 0%,#0c1020 100%);color:var(--txt)}
    .sidebar{width:270px;background:linear-gradient(180deg,#13182b 0%,#0f1425 100%);border-right:1px solid var(--line);padding:24px 16px;position:sticky;top:0;height:100vh;display:flex;flex-direction:column;gap:16px}
    .brand{display:flex;align-items:center;gap:10px;font-weight:700;font-size:18px;padding:8px 12px;border-radius:12px;background:rgba(124,92,255,.12)}
    .brand img{width:28px;height:28px}
    nav{display:flex;flex-direction:column;gap:8px;margin-top:8px}
    nav a, nav button.linkish{display:flex;align-items:center;gap:10px;padding:10px 12px;border-radius:10px;text-decoration:none;color:#cbd3e7;background:transparent;border:0;cursor:pointer;text-align:left}
    nav a:hover, nav button.linkish:hover{background:#1a2034;color:#fff}
    nav button.danger{color:#ff9595}
    .content{flex:1;display:flex;flex-direction:column;min-width:0}
    .topbar{display:flex;align-items:center;justify-content:space-between;padding:18px 24px;border-bottom:1px solid var(--line);background:rgba(17,21,36,.6);backdrop-filter:blur(6px);position:sticky;top:0;z-index:5}
    .topbar h1{margin:0;font-size:18px}
    .me{opacity:.9}
    .page{padding:22px;display:block}

    .cards{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:16px;margin-bottom:22px}
    .card{background:var(--panel);border:1px solid var(--line);border-radius:14px;padding:16px}
    .kpi{display:flex;flex-direction:column;gap:6px}
    .kpi .v{font-size:28px;font-weight:700}
    .kpi .l{color:var(--muted);font-size:12px}

    .table{width:100%;border-collapse:collapse;background:var(--panel);border:1px solid var(--line);border-radius:14px;overflow:hidden}
    .table th,.table td{padding:12px 14px;border-bottom:1px solid var(--line);font-size:14px;vertical-align:middle}
    .table th{background:#182038;color:#aeb7cc;text-align:left}

    .badge{padding:4px 10px;border-radius:999px;font-size:12px;white-space:nowrap;border:1px solid #283152}
    .b-active{background:rgba(32,201,151,.15);color:#20c997}
    .b-upcoming{background:rgba(124,92,255,.15);color:#b29aff}
    .b-expired,.b-finished{background:rgba(255,107,107,.12);color:#ff7a7a}

    .actions{display:flex;gap:8px;flex-wrap:wrap}
    .btn{padding:8px 12px;border-radius:10px;border:1px solid var(--line);background:#1b2135;color:#e7eefe;text-decoration:none;cursor:pointer}
    .btn:hover{filter:brightness(1.1)}
    .btn.ghost{background:transparent}
    .btn.ok{border-color:#244c3f;background:#17312a}
    .btn.bad{border-color:#4c2424;background:#2d1717}

    .grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}
    .form{background:var(--panel);border:1px solid var(--line);border-radius:14px;padding:16px}
    label{display:block;font-size:12px;color:#aeb7cc;margin-bottom:6px}
    input,select,textarea{width:100%;padding:10px 12px;border-radius:10px;border:1px solid var(--line);background:#111729;color:#e6e9ef}
    textarea{min-height:96px;resize:vertical}
    .mt{margin-top:14px}
    .inline{display:flex;gap:8px;align-items:center}
    .panel{background:var(--panel);border:1px solid var(--line);border-radius:14px;padding:16px;display:flex;flex-direction:column;gap:12px}
    .panel-row{display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap}
    .muted{color:var(--muted);font-size:12px}

    .grid-numbers{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:8px}
    .num{background:#131a2d;border:1px solid #202847;border-radius:10px;padding:12px;text-align:center}
    .num.reserved{opacity:.65;border-style:dashed}
    .num.sold{opacity:.4;filter:grayscale(0.3)}

    .progress-wrap{display:flex;align-items:center;gap:10px}
    .progress{height:8px;background:#101737;border:1px solid #2a3354;border-radius:999px;overflow:hidden;width:160px}
    .progress>span{display:block;height:100%;background:linear-gradient(90deg,#22d3ee,#7c5cff)}

    @media (max-width:1200px){.cards{grid-template-columns:repeat(2,minmax(0,1fr))}}
    @media (max-width:820px){
      .sidebar{width:100%;height:auto;position:static}
      .topbar{position:static}
      .cards{grid-template-columns:1fr}
      .grid{grid-template-columns:1fr}
      .grid-numbers{grid-template-columns:repeat(3,1fr)}
    }
  </style>
</head>
<body>
  <aside class="sidebar">
    <div class="brand"><img src="https://cdn.jsdelivr.net/gh/tabler/tabler-icons/icons/brand-firebase.svg" alt="">Rifa Admin</div>
    <nav>
      <a href="/admin-ui">Dashboard</a>
      <a href="/admin-ui/campaigns/new">Nova Campanha</a>
      <form action="/admin-ui/expire-reservations" method="post"><button type="submit" class="linkish">Expirar Reservas</button></form>
      <form action="/admin-ui/logout" method="post"><button type="submit" class="linkish danger">Sair</button></form>
    </nav>
  </aside>
  <main class="content">
    <header class="topbar">
      <h1><%= title || 'Dashboard' %></h1>
      <div class="me">👤 <%= admin ? admin.username : "" %></div>
    </header>
    <section class="page"><%- body %></section>
  </main>
</body>
</html>
