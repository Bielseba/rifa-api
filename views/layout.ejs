<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title ? title + " · Admin" : "Painel Admin" %></title>
  <link rel="stylesheet" href="/public/css/admin.css" />
  <style>
    :root {
      --bg-dark: #1f1f1f;
      --bg-light: #f8f9fa;
      --primary: #007bff;
      --danger: #ff4747;
      --text: #333;
    }

    * {
      box-sizing: border-box;
      font-family: "Poppins", sans-serif;
    }

    body {
      margin: 0;
      display: flex;
      height: 100vh;
      color: var(--text);
      background: var(--bg-light);
    }

    .sidebar {
      background: var(--bg-dark);
      color: white;
      width: 240px;
      display: flex;
      flex-direction: column;
      padding: 1.5rem 1rem;
    }

    .brand {
      font-size: 1.3rem;
      font-weight: bold;
      text-align: center;
      margin-bottom: 2rem;
    }

    nav {
      display: flex;
      flex-direction: column;
      gap: 0.8rem;
    }

    nav a {
      color: #ddd;
      text-decoration: none;
      padding: 0.5rem 0.8rem;
      border-radius: 6px;
      transition: background 0.2s;
    }

    nav a:hover {
      background: rgba(255, 255, 255, 0.1);
      color: #fff;
    }

    form {
      margin: 0;
    }

    .linkish,
    .danger {
      background: none;
      border: none;
      color: #ccc;
      text-align: left;
      width: 100%;
      padding: 0.5rem 0.8rem;
      border-radius: 6px;
      cursor: pointer;
      font-size: 0.95rem;
    }

    .danger {
      color: var(--danger);
    }

    .linkish:hover {
      background: rgba(255, 255, 255, 0.1);
    }

    .content {
      flex: 1;
      display: flex;
      flex-direction: column;
    }

    .topbar {
      background: #fff;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 2rem;
      border-bottom: 1px solid #ddd;
    }

    .topbar h1 {
      margin: 0;
      font-size: 1.2rem;
      font-weight: 600;
      color: #111;
    }

    .me {
      font-weight: 500;
      color: #555;
    }

    .page {
      padding: 2rem;
      overflow-y: auto;
      flex: 1;
      background: #fafafa;
    }

    @media (max-width: 768px) {
      .sidebar {
        display: none;
      }
      body {
        flex-direction: column;
      }
      .content {
        width: 100%;
      }
      .topbar {
        justify-content: center;
      }
    }
  </style>
</head>
<body>
  <aside class="sidebar">
    <div class="brand">🎟️ Rifa Admin</div>
    <nav>
      <a href="/admin-ui">🏠 Dashboard</a>
      <a href="/admin-ui/campaigns/new">➕ Nova Campanha</a>
      <form action="/admin-ui/expire-reservations" method="post">
        <button type="submit" class="linkish">⏳ Expirar Reservas</button>
      </form>
      <form action="/admin-ui/logout" method="post">
        <button type="submit" class="danger">🚪 Sair</button>
      </form>
    </nav>
  </aside>

  <main class="content">
    <header class="topbar">
      <h1><%= title %></h1>
      <div class="me">👤 <%= admin ? admin.username : "" %></div>
    </header>
    <section class="page">
      <%- body %>
    </section>
  </main>
</body>
</html>
