{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/admin-ui/(.*)", "dest": "server.js" },
    { "src": "/admin/(.*)", "dest": "server.js" },
    { "src": "/auth/(.*)", "dest": "server.js" },
    { "src": "/campaigns/(.*)", "dest": "server.js" },
    { "src": "/user/(.*)", "dest": "server.js" },
    { "src": "/purchases/(.*)", "dest": "server.js" },
    { "src": "/winners/(.*)", "dest": "server.js" },
    { "src": "/webhooks/(.*)", "dest": "server.js" },
    { "src": "/public/(.*)", "dest": "server.js" },
    { "src": "/(.*)", "dest": "server.js" }
  ],
  "env": {
    "DATABASE_URL": "postgresql://neondb_owner:npg_c0GiBvAkM2jh@ep-curly-darkness-adse41g5-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require",
    "JWT_SECRET": "change_me_to_a_long_random_secret",
    "RESERVATION_MINUTES": "10",
    "CORS_ORIGIN": "*"
  }
}
